spring:
  datasource:
    url: jdbc:postgresql://docker-postgres:5432/todo
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create


server:
  port: 8080

jwt:
  secret: 'letscode'
